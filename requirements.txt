# ===========================================
# K-Maths Production Requirements
# Optimized for Docker deployment on VPS
# ===========================================

# Always use CPU-only PyTorch wheels (no CUDA runtime packages)
--extra-index-url https://download.pytorch.org/whl/cpu

# Core UI Framework
PySide6>=6.4.0

# Database
mysql-connector-python>=8.0.0

# Security
bcrypt>=4.0.0

# PDF Processing
PyMuPDF>=1.21.0

# OCR Dependencies (Production Only)
# Using lighter OCR stack for production
easyocr>=1.6.0
paddlepaddle>=3.0.0
paddleocr>=2.8.0
pytesseract>=0.3.10
torch==2.10.0+cpu
torchvision==0.25.0+cpu
numpy>=1.23.0

# System Monitoring
psutil>=5.9.0

# Web Interface + Sync API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.4
python-multipart>=0.0.9
PyJWT>=2.8.0
itsdangerous>=2.2.0
weasyprint>=62.3
